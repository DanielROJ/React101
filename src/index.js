/* Prueba Nro. 1
 * 1.) Impremente un nuevo botón que haga un Reset a Cero (0), al
 *     contador de la clase.
 * Prueba Nro. 2
 *  1.) Optimice el siguiente  componente de clase  para que solo tenga
 *     un método de sincronización, para actualizar los valores de los
 *     identificadores email y password en el estado del componente
 * Prueba Nro. 3
 * 1.) Revise y entienda que hace el siguiente componente
 * 2.) Usando cualquier técnica de maquetación web, mejore
 *     la presentación de lso datos al usuario
 * 3.) Resuelva el Warning:
 *     Each child in a list should have a unique "key" prop.
 *
 * Bonus Track
 *  1.) Elaborar un menú de botones que permita acceder a las tres pruebas
 */

import React from "react";
import ReactDOM from "react-dom";

//import App from "./Prueba01";
//import App from "./Prueba02";
//import App from "./Prueba03";
import App from "./ControlPruebas";

ReactDOM.render(<App />, document.getElementById("root"));
